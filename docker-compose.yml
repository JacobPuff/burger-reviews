version: "3.8"
services:
  burger-reviews-redis:
    image: redis:6.0.10
    volumes:
      - "./redis.conf:/usr/local/etc/redis/redis.conf"
  burger-reviews:
    build: .
    ports:
      - "9090:9090"
    environment:
      - SERVER_DOMAIN=jacob.squizzlezig.com
      - SERVER_PORT=9090
      - REDIS_HOST=burger-reviews-redis
      - DEV_MODE=dev
    volumes:
      - "./constants.json:/app/backend/constants.json"
    links:
      - "burger-reviews-redis"
    depends_on:
      - "burger-reviews-redis"